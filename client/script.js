// EventListeners

addUserForm.addEventListener("submit", onSumbit);

const userContainerElement = document.getElementById('cardContainer');
const api = new Api("http://localhost:5000/users");

// InputValidation

// SaveFunc

// EditFunc

// DeleteFunc

// RenderFunc